numpy>=1.26,<3
scipy>=1.11,<2
cvxpy>=1.4,<2
pandas>=2.1,<3
pyarrow>=15,<21
scikit-learn>=1.3,<2
statsmodels>=0.14,<1
# taxcalc is PINNED EXACTLY (not a range like the others): it is the federal-tax
# authority (models/tax/calculator.compute_tax), and Gate 1 (the CI tax-oracle job,
# scripts/validate_tax/gate1.py) is adjudicated to 0-investigate against THIS version —
# federal agrees with PolicyEngine-US to the penny. taxcalc 6.6.x diverges from
# PolicyEngine on HIGH-INCOME federal (≥$640k it under-taxes by a constant ~$3,965
# single/HoH, ~$6,750 MFJ — a policy_current_law.json change PolicyEngine has not
# adopted), breaking the penny-exact invariant and tripping 42 federal "investigate"
# disagreements that block CI. Bump in lockstep with the oracle pin in
# .github/workflows/ci.yml + a Gate-1 re-adjudication.
# TODO(taxcalc-6.6): assess whether 6.6.x's high-income change is a real 2026-law
# correction to adopt (then re-baseline gate1_report.md), or a taxcalc regression.
taxcalc==6.5.2
